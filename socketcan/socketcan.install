# arg 1:  the new package version
# arg 2:  the old package version

/sbin/depmod

post_install () {
  echo "Make sure to 'modprobe can' to load the module"
}

post_upgrade() {
  echo "Make sure to 'modprobe can' to load the module"
}

op=$1
shift

$op $*
